# 四福车间管理系统 - 使用说明

## 一、快速启动

1. 双击 `start.bat` 启动系统（黑色窗口保持开启即为运行中）
2. 浏览器打开 http://localhost:8090
3. 初始账号：`admin`　初始密码：`admin123`（登录后请在「用户管理」中修改）

> 手机/其他电脑访问：在同一局域网内，浏览器输入 `http://本机IP:8090`
> （本机IP查看方法：黑色窗口所在电脑上运行 `ipconfig`，找 IPv4 地址）

## 二、系统组成

### 模块1：铜线出入库台账
| 页面 | 功能 |
|---|---|
| 生产对比表 | 生产车间每日生产型号重量对比：日期、任务单号、BOM号、规格（**一个任务单可录多种漆包线规格**）、BOM重量、实际重量、自动算差重、备注 |
| 仓库铜线领用表 | 仓库管理员填写**实际领用情况**：日期、生产任务单号、漆包线规格、领出重量、回库重量、线头报废重量（自动算实际用量）、备注。与「生产对比表」相互独立，不记录 BOM 差重 |

两张表都支持：**按日期/单号/规格查询、打印（含签字栏）、导出 Excel**。

### 模块2：小型工具管理台账
| 页面 | 功能 |
|---|---|
| 工具档案 | 每件工具一张"身份证"：自动编号（类别+流水号，如 DZ-001）、序列号、品牌型号、位置、采购信息、照片、安全库存、保养周期；支持**批量打印二维码标签** |
| 借用归还 | 扫码枪/手工输入编号 → 自动识别借用或归还；归还时检查状态（完好/待维修/已损坏），损坏自动转维修 |
| 维修报废 | 维修保养登记（费用、原因）、完成后自动回到在库；报废登记（原因+批准人）自动移出在册库存 |
| 盘点 | 一键生成盘点单，扫码逐件清点，结束后自动生成**盘盈盘亏报告** |
| 统计报表 | 个人未还清单、工具周转率/使用率、维修报废统计 |
| 总览看板 | 库存实时看板、**逾期未还提醒**、**安全库存预警**、**保养到期提醒**、今日铜线台账速览 |

## 三、用户角色

| 角色 | 可见模块 |
|---|---|
| 系统管理员 | 全部模块 + 用户管理 + 数据备份与恢复 |
| 生产车间 | 仅「生产对比表」（仓库领用表仅管理员可见） |
| 仓库管理员 | 仅「小型工具管理台账」模块（工具档案、借用归还、维修报废、盘点、统计报表） |

> 权限为前后端双重校验：非授权模块在导航栏中不显示，直接输入网址访问会自动跳回总览；调用接口会返回 403 拒绝。

## 四、扫码说明

- **扫码枪**（推荐，即插即用）：光标点在"工具编号"输入框内，扫描工具上的二维码标签即可，无需任何设置。
- **二维码标签**：在「工具档案」勾选工具 → 点「打印二维码标签」，用普通打印机或标签机打印后贴在工具上。
- 手机在局域网内打开系统后同样可手工输入编号操作。

## 五、数据安全

- 所有数据保存在本目录 `data.db` 单文件中（SQLite，免费、无需安装任何数据库软件）
- **自动备份**：系统每天 **15:00** 自动将数据库备份到本目录 `backups/` 文件夹，文件名形如 `auto-YYYYMMDD-HHMMSS.db`，自动保留最近 **30** 份（更早的自动清理），无需人工干预。
- **手动备份**：管理员在「用户管理 → 数据备份与恢复」点「下载当前备份」，或总览看板点「备份数据库」，浏览器直接下载 `backup-日期.db`。
- **数据恢复**（仅管理员）：在「用户管理 → 数据备份与恢复」中：
  - 「选择备份文件恢复」：从本机选一个 `.db` 备份文件上传恢复；
  - 自动备份列表里对每个备份可「下载 / 恢复 / 删除」一键操作。
  - 恢复前系统会**先自动备份当前数据**（存为 `pre-restore-时间戳.db`），万一恢复有误可再次用该文件回退。
- 也可直接复制 `data.db` 与 `data.db-wal / data.db-shm` 一并存档；恢复时把备份文件改名为 `data.db`（同时覆盖 WAL/SHM）覆盖即可。

## 六、常见问题

- 端口被占用：编辑 start.bat，在 node 前加 `set PORT=8091 &&` 换端口
- 忘记管理员密码：删除 data.db 会重建全部数据（慎用），建议提前多建一个管理员账号
